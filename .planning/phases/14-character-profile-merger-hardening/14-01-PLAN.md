---
phase: 14-character-profile-merger-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/attribution/models.py
  - src/attribution/extractor.py
  - tests/test_merger.py
autonomous: true
requirements:
  - MERGE-01
  - MERGE-03
  - MERGE-08

must_haves:
  truths:
    - "MergeDecision and MergeAudit models exist and are importable"
    - "Extraction prompt contains explicit negative examples for family-name confusion"
    - "Test scaffold covers all 8 MERGE requirements with failing tests"
  artifacts:
    - path: "src/attribution/models.py"
      provides: "MergeDecision and MergeAudit Pydantic models"
      contains: "class MergeDecision"
    - path: "src/attribution/extractor.py"
      provides: "Hardened extraction prompt with negative examples"
      contains: "WRONG"
    - path: "tests/test_merger.py"
      provides: "Test scaffold for MERGE-01 through MERGE-08"
      contains: "TestAuditTrail"
  key_links:
    - from: "tests/test_merger.py"
      to: "src/attribution/models.py"
      via: "import MergeDecision, MergeAudit"
      pattern: "from src\\.attribution\\.models import.*MergeDecision"
    - from: "tests/test_merger.py"
      to: "src/attribution/merger.py"
      via: "import merge_characters"
      pattern: "from src\\.attribution\\.merger import"
---

<objective>
Create the data contracts (MergeDecision, MergeAudit models), test scaffold for all MERGE requirements, and harden the extraction prompt with negative examples.

Purpose: Establish the foundation that Plan 02 implements against -- models define the audit trail shape, tests define expected behavior, and prompt hardening reduces upstream garbage before the merger even runs.
Output: MergeDecision/MergeAudit models in models.py, comprehensive test_merger.py with tests for MERGE-01 through MERGE-08, hardened EXTRACTION_SYSTEM_PROMPT in extractor.py.
</objective>

<execution_context>
@/Users/rhodesfotopulos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rhodesfotopulos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-character-profile-merger-hardening/14-RESEARCH.md

@src/attribution/models.py
@src/attribution/extractor.py
@src/attribution/merger.py
@tests/test_dedup.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/attribution/models.py:
```python
class VoiceProfile(BaseModel):
    pitch: str
    pace: str
    tone: str
    accent: str
    pace_style: str
    tone_style: str
    energy: str
    typical_emotion: str
    description: str

class CharacterProfile(BaseModel):
    name: str
    aliases: list[str] = []
    gender: str
    age_range: str
    voice_profile: VoiceProfile
    personality_traits: list[str] = []
    description: str
    is_named: bool

class ConsolidationResult(BaseModel):
    groups: list[DuplicateGroup]

class DuplicateGroup(BaseModel):
    indices: list[int]
    canonical_index: int
    reasoning: str
```

From src/attribution/merger.py:
```python
def merge_characters(
    chapter_characters: dict[int, list[CharacterProfile]],
    cache_dir: Path | None = None,
) -> list[CharacterProfile]:
```

From src/attribution/extractor.py (line 43):
```python
EXTRACTION_SYSTEM_PROMPT = """\
You are a character extraction specialist for audiobook production. ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MergeDecision/MergeAudit models and write test scaffold</name>
  <files>src/attribution/models.py, tests/test_merger.py</files>
  <behavior>
    - MergeDecision has fields: stage (str), profile_a (str), profile_b (str), action (str), reason (str), confidence (float), details (dict, default empty)
    - MergeAudit has fields: book_title (str), timestamp (str), total_raw_profiles (int), total_merged_profiles (int), stages (dict[str, list[MergeDecision]]), warnings (list[str]), final_profiles (list[str])
    - MergeAudit.model_dump() produces valid JSON-serializable dict
    - TestAuditTrail: merge_characters returns audit trail when passed an audit collector (MERGE-01)
    - TestPostMergeValidation: profiles with >5 aliases, >50 traits, or alias matching another canonical name produce warnings (MERGE-02)
    - TestAuditOutput: merge_audit.json is written alongside characters.json (MERGE-03)
    - TestCrossNameExclusion: alias that matches another profile's canonical name is blocked (MERGE-04)
    - TestCooccurrenceSignal: co-occurrence data is passed to LLM as signal, not used as binary gate (MERGE-05)
    - TestTraitCap: profiles with >30 traits after merge are capped, overflow discarded (MERGE-06)
    - TestSurnameExclusion: profiles sharing surname but different titles/first-names are not merged (MERGE-07)
    - TestExtractionPrompt: EXTRACTION_SYSTEM_PROMPT contains "WRONG" negative examples (MERGE-08)
  </behavior>
  <action>
1. Add MergeDecision and MergeAudit Pydantic models to src/attribution/models.py at the end of the file, after ConsolidationResult. Follow the schema from 14-RESEARCH.md:
   - MergeDecision: stage (str), profile_a (str), profile_b (str), action (str "merged"|"rejected"|"flagged"), reason (str), confidence (float 0.0-1.0), details (dict = {})
   - MergeAudit: book_title (str), timestamp (str), total_raw_profiles (int), total_merged_profiles (int), stages (dict[str, list[MergeDecision]] = {}), warnings (list[str] = []), final_profiles (list[str] = [])

2. Create tests/test_merger.py with comprehensive test classes. Use a helper function `_make_profile(name, aliases=[], traits=[], is_named=True, gender="unknown", age_range="unknown")` that creates a CharacterProfile with sensible VoiceProfile defaults (all "unknown"). Pattern follows test_dedup.py's `_make_assignment` helper style.

Test classes (all tests should currently FAIL or be marked with expectations that will pass once Plan 02 implements the functionality):

- TestAuditTrail: Tests that merge_characters records decisions. Since merge_characters doesn't yet return/accept audit, write tests against the NEW signature that Plan 02 will implement: `merge_characters(chapter_characters, cache_dir, audit)`. For now, test that MergeDecision and MergeAudit models can be constructed and serialized correctly. Add a test that calls the CURRENT merge_characters to verify it still works (regression).

- TestPostMergeValidation: Test _validate_merged_profiles (function to be added in Plan 02). Write tests as standalone validation logic tests using the MergeAudit model -- create profiles that violate thresholds and assert warnings are generated. Mark as pytest.mark.skip("Plan 02: _validate_merged_profiles not yet implemented") for tests that call not-yet-existing functions.

- TestAuditOutput: Test that merge_audit.json can be written from MergeAudit.model_dump(). Test JSON round-trip serialization.

- TestCrossNameExclusion: Create two profiles where one's alias matches the other's canonical name. Assert the alias is blocked. Mark skip for Plan 02 implementation.

- TestCooccurrenceSignal: Test _build_cooccurrence still works. Test that co-occurring profiles are NOT automatically blocked (the new behavior). Mark skip for LLM arbiter tests.

- TestTraitCap: Create a profile with 40 traits, merge with another. Assert result has <= 30. Mark skip for Plan 02 _merge_two_profiles changes.

- TestSurnameExclusion: Test _names_share_surname_only with edge cases: "Mr. Bennet" vs "Mrs. Bennet" (True), "Mr. Darcy" vs "Darcy" (False -- single-part name), "Miss Darcy" vs "Georgiana Darcy" (True -- different prefix). These test the EXISTING function, so they should pass now.

- TestExtractionPrompt: Assert EXTRACTION_SYSTEM_PROMPT contains "WRONG" substring (will pass after Task 2).

Keep all tests that test existing code passing. Tests for Plan 02 features should be skipped with clear reason strings.
  </action>
  <verify>
    <automated>python -m pytest tests/test_merger.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>MergeDecision and MergeAudit models importable from src.attribution.models. tests/test_merger.py has 8+ test classes covering MERGE-01 through MERGE-08. Tests against existing code pass; tests for Plan 02 features are skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Harden extraction prompt with negative examples (MERGE-08)</name>
  <files>src/attribution/extractor.py</files>
  <action>
Add explicit negative examples to EXTRACTION_SYSTEM_PROMPT in extractor.py (line 43). Insert a new section BEFORE the "Rules:" section with concrete WRONG and RIGHT examples. Content per research:

```
WRONG examples (DO NOT do this):
- Mrs. Bennet with aliases=["Elizabeth", "Jane", "Lydia"]
  (These are her DAUGHTERS, not aliases for Mrs. Bennet)
- Mr. Darcy with aliases=["Miss Darcy", "Georgiana"]
  (Miss Darcy/Georgiana is his SISTER, a separate character)
- Elizabeth with aliases=["Mrs. Bennet"]
  (Mrs. Bennet is Elizabeth's MOTHER, not an alias)
- "the doctor" with aliases=["Dr. Smith"]
  (Only merge if the text confirms they are the same person)
- Letter recipients: "Dear Jane" in a letter FROM Elizabeth does NOT make Jane an alias of Elizabeth

RIGHT examples:
- Elizabeth Bennet with aliases=["Lizzy", "Eliza", "Miss Bennet"]
  (These are all names for the same person)
- Mr. Darcy with aliases=["Fitzwilliam Darcy", "Darcy"]
  (These are all names for the same person)
- Mrs. Bennet with aliases=[] (no aliases -- other Bennets are separate characters)
```

Also update the cache key namespace: change "extraction" to "extraction_v3" in the extract function's get_cache_key call (currently "extraction_v2" from a prior version bump). This ensures cached results from the old prompt are invalidated.
  </action>
  <verify>
    <automated>python -m pytest tests/test_merger.py::TestExtractionPrompt -x -v && python -c "from src.attribution.extractor import EXTRACTION_SYSTEM_PROMPT; assert 'WRONG' in EXTRACTION_SYSTEM_PROMPT; print('OK')"</automated>
  </verify>
  <done>EXTRACTION_SYSTEM_PROMPT contains WRONG/RIGHT negative examples for family-name confusion. Cache key bumped to extraction_v3 to invalidate stale cached extractions.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_merger.py -x -v` -- all non-skipped tests pass
- `python -c "from src.attribution.models import MergeDecision, MergeAudit; print('Models OK')"` -- models importable
- `python -c "from src.attribution.extractor import EXTRACTION_SYSTEM_PROMPT; assert 'WRONG' in EXTRACTION_SYSTEM_PROMPT; print('Prompt OK')"` -- prompt hardened
</verification>

<success_criteria>
- MergeDecision and MergeAudit models exist in models.py and are JSON-serializable via model_dump()
- tests/test_merger.py has test classes for all 8 MERGE requirements
- Existing merger.py tests (TestSurnameExclusion, TestCooccurrenceSignal basics) pass
- EXTRACTION_SYSTEM_PROMPT contains WRONG/RIGHT negative examples
- Cache namespace bumped to avoid stale extraction results
</success_criteria>

<output>
After completion, create `.planning/phases/14-character-profile-merger-hardening/14-01-SUMMARY.md`
</output>
