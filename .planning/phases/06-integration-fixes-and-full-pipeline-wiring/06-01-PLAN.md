---
phase: 06-integration-fixes-and-full-pipeline-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assembly/assembler.py
  - src/assembly/announcer.py
  - src/matching/orchestrator.py
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
  - VOICE-01
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - VOICE-05

must_haves:
  truths:
    - "Assembler discovers WAV files written by synthesizer in wavs/ch{NN}/seg_{NNNN}.wav format"
    - "Announcer handles missing/placeholder narrator clip_path by returning empty dict instead of crashing"
    - "CharacterProfile and VoiceMap use model_validate() for robust nested dict coercion"
  artifacts:
    - path: "src/assembly/assembler.py"
      provides: "Fixed WAV path convention matching synthesizer output"
      contains: "ch{ch_num:02d}"
    - path: "src/assembly/announcer.py"
      provides: "Graceful degradation when narrator clip is placeholder"
      contains: "return {}"
    - path: "src/matching/orchestrator.py"
      provides: "Pydantic v2 best-practice model loading"
      contains: "model_validate"
  key_links:
    - from: "src/synthesis/synthesizer.py"
      to: "src/assembly/assembler.py"
      via: "WAV path convention"
      pattern: "ch\\d{2}.*seg_\\d{4}\\.wav"
    - from: "src/matching/orchestrator.py"
      to: "src/assembly/announcer.py"
      via: "narrator clip_path from voice_map.json"
      pattern: "AUDIO_DIR/"
---

<objective>
Fix three integration bugs that prevent Phase 3-5 code from working correctly: (1) WAV path mismatch between synthesizer and assembler, (2) announcer crash on placeholder narrator clip_path, (3) Pydantic model loading best-practice fix.

Purpose: These are the leaf-level data/code bugs. They must be fixed before the pipeline can be wired end-to-end in Plan 02.
Output: Three files patched with targeted fixes — no new files, no new dependencies.
</objective>

<execution_context>
@/Users/rhodesfotopulos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rhodesfotopulos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/06-integration-fixes-and-full-pipeline-wiring/06-RESEARCH.md
@src/synthesis/synthesizer.py (REFERENCE ONLY — the WAV path convention source of truth: wavs/ch{ch_num:02d}/seg_{seg_id:04d}.wav)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WAV path convention in assembler.py</name>
  <files>src/assembly/assembler.py</files>
  <action>
Fix THREE locations in assembler.py where the WAV path convention diverges from the synthesizer's format:

1. **Line 74 — input validation glob**: Change `any(wavs_dir.glob("*.wav"))` to `any(wavs_dir.glob("ch*/*.wav"))`. The synthesizer writes WAVs into chapter subdirectories (`wavs/ch00/`, `wavs/ch01/`, etc.), not directly into `wavs/`.

2. **Lines 184-189 — segment WAV lookup**: Replace the flat path construction:
   ```python
   # CURRENT (broken):
   wav_path = wavs_dir / f"segment_{seg_id:06d}.wav"
   ```
   with chapter-organized path matching the synthesizer convention:
   ```python
   # FIXED:
   ch_num_for_path = seg.get("chapter", 0)
   wav_path = wavs_dir / f"ch{ch_num_for_path:02d}" / f"seg_{seg_id:04d}.wav"
   ```
   Note the format changes: `segment_` -> `seg_`, `06d` -> `04d`, and the chapter subdirectory prefix. These match exactly what `synthesizer.py` lines 196-199 produce.

3. **Keep the existing warning log** for missing WAVs on line 191 — the assembler should still gracefully skip missing segments.

Do NOT change any other logic in the assembler. The concatenation, normalization, encoding, and tagging code is all correct — it just never ran because it received zero WAV files.
  </action>
  <verify>
Run: `python -c "from src.assembly.assembler import run_assembly; print('import OK')"`
Grep the file to confirm all three fixes: `grep -n 'ch.*wav\|glob.*ch' src/assembly/assembler.py`
Expected: line 74 shows `glob("ch*/*.wav")`, lines ~187 show `ch{ch_num_for_path:02d}` and `seg_{seg_id:04d}.wav`
  </verify>
  <done>assembler.py uses the synthesizer's WAV path convention (wavs/ch{NN}/seg_{NNNN}.wav) in both validation and segment lookup — no more flat wavs/segment_{NNNNNN}.wav references</done>
</task>

<task type="auto">
  <name>Task 2: Fix announcer placeholder crash and orchestrator model_validate</name>
  <files>src/assembly/announcer.py, src/matching/orchestrator.py</files>
  <action>
**announcer.py — graceful degradation for placeholder clip_path:**

Replace the FileNotFoundError raise on lines 45-49 with a warning-and-return pattern matching how the synthesizer handles placeholders (synthesizer.py line 202):

```python
# Replace lines 45-49:
if not narrator_ref_path.exists():
    # Placeholder path from matching without LibriTTS-R audio — skip announcements
    logger.warning(
        "Narrator reference clip not found: %s — skipping chapter announcements. "
        "Provide --libritts-audio to enable announcements.",
        narrator_ref_path,
    )
    return {}
```

Also check if the path string starts with `"AUDIO_DIR/"` as an additional detection (matching synthesizer.py line 202). Add this check BEFORE the `.exists()` check:

```python
# Check for known placeholder pattern (from matching without --libritts-audio)
if str(narrator_ref_path).startswith("AUDIO_DIR/") or not narrator_ref_path.exists():
    logger.warning(...)
    return {}
```

Remove the `FileNotFoundError` from the docstring's Raises section since we no longer raise it.

**orchestrator.py — model_validate() best practice:**

1. Line 64: Change `return VoiceMap(**data)` to `return VoiceMap.model_validate(data)` (cache-hit path)
2. Line 74: Change `characters = [CharacterProfile(**c) for c in characters_data]` to `characters = [CharacterProfile.model_validate(c) for c in characters_data]`

These are Pydantic v2 best-practice fixes. While `**data` works on Pydantic 2.12.5, `model_validate()` is more explicit about intent and handles edge cases better.
  </action>
  <verify>
Run: `python -c "from src.assembly.announcer import generate_announcements; print('import OK')"`
Run: `python -c "from src.matching.orchestrator import run_matching; print('import OK')"`
Grep: `grep -n 'model_validate' src/matching/orchestrator.py` — should show lines 64 and 74
Grep: `grep -n 'AUDIO_DIR\|return {}' src/assembly/announcer.py` — should show placeholder check and empty return
  </verify>
  <done>Announcer returns empty dict when narrator clip is placeholder/missing instead of raising FileNotFoundError. Orchestrator uses model_validate() for CharacterProfile and VoiceMap loading.</done>
</task>

</tasks>

<verification>
1. `python -c "from src.assembly.assembler import run_assembly; from src.assembly.announcer import generate_announcements; from src.matching.orchestrator import run_matching; print('All imports OK')"` — all three fixed modules import without error
2. `grep -c 'segment_.*06d' src/assembly/assembler.py` — returns 0 (no old flat path references)
3. `grep -c 'model_validate' src/matching/orchestrator.py` — returns 2 (both fixes applied)
4. `grep -c 'FileNotFoundError' src/assembly/announcer.py` — returns 0 (no longer raised)
</verification>

<success_criteria>
- assembler.py validates and discovers WAVs using `ch{NN}/seg_{NNNN}.wav` convention matching synthesizer output
- announcer.py returns empty dict (no crash) when narrator clip_path is a placeholder or missing file
- orchestrator.py uses `model_validate()` for both CharacterProfile and VoiceMap construction
- All three modules import cleanly with no syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-fixes-and-full-pipeline-wiring/06-01-SUMMARY.md`
</output>
