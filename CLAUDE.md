# Audio Book Plz

## GSD Commands

Always use `/gsd:*` commands from the local `.claude/commands/gsd/` directory in this project, not from the global `~/.claude/commands/gsd/` directory. If both are available, prefer the local versions.
